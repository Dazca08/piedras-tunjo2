import { FiltropromPipe } from './filtroprom.pipe';

describe('FiltropromPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltropromPipe();
    expect(pipe).toBeTruthy();
  });
});
