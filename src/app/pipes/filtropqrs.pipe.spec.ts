import { FiltropqrsPipe } from './filtropqrs.pipe';

describe('FiltropqrsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltropqrsPipe();
    expect(pipe).toBeTruthy();
  });
});
